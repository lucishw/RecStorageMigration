# F-11 — Runtime·Interval (행위 추출)

- Before batch + each Interval second: IsCurrentTimeInSchedule weekday/weekend
- Out of runtime → message「Runtime 범위 외 중지」, exit worker
- Interval: countdown status「Interval 대기 중.. N」
