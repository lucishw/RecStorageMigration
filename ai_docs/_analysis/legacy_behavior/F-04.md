# F-04 — Source DB (행위 추출)

- Connection string: Server, Database, UID, PWD; Trust adds Encrypt=True;TrustServerCertificate=False
- Mark table: SourceDBNM.dbo.MarkTable or MarkTable_YYYY when YearTable marking
- Mark UPDATE: MarkField, ETL_DT, optional AES256 DialedNumber/CallerNumber
- WHERE FileName + ServerName
