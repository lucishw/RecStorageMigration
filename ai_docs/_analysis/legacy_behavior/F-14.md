# F-14 — RoboCopy (행위 추출)

- Manual tab: paths, patterns, robocopy options, preview command
- Process async with output queue flush to listbox
- Mirror warning; timeout kill; exit code <= max success
- Optional report log + summary.txt + open folder
