# F-05 — 경로 (행위 추출)

- I360: row virtual path → longest Virtual→Real mapping → target Real→TargetPath
- Audiolog: path + optional AddField + server + filename segments + yyyymmdd
- PathMapping INI: Enabled, VirtualPath, RealSourcePath, Memo; legacy I360IIS/I360NetBios fallback
