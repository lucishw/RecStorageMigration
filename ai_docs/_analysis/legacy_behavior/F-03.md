# F-03 — 스케줄러 (행위 추출)

- tsbStart: scheduler on, Config disabled, tmrBatch 1s, _Work=true
- tsbStop: timer off, StopAllWorkers
- tick: _Wait++ until Interval; then if Runtime → StartAllWorkers(checked, not running); else status out of schedule
- RetryWork/_RetryCycle set in tick but legacy batch unused
