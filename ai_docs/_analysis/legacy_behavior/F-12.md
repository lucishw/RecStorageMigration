# F-12 — 상태·통계 (행위 추출)

- Progress events queued; timer flush to UI (interval ms from INI)
- Stats save every WorkerStatsSaveIntervalSec; force on finish
- Labels: 처리일자 separate from status; Count processed/success/failure
- ProgressBar marquee while running
