# F-13 — 로그·리포트 (행위 추출)

- Split log main left / worker right; radio per worker
- Optional all-worker log grid
- Worker log files worker_n_date_app.log / Err
- Report dialog: summary totals + per-worker TSV rows + copy
