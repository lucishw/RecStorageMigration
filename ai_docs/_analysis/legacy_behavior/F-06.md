# F-06 — Multi Worker UI (행위 추출)

- Row: Wn checkbox, Top, Interval, From/To, Mark, Where, Config DB, DBName, Year Table, buttons
- FlowLayout 2-col when width >= ~1360; min panel ~660
- Add/delete/select all/reset totals/report; delete confirm; no delete if running
- INI Workers Count, WorkerIds, NextWorkerId; per Worker_n keys
