# F-01 — 프로그램 시작 (행위 추출)

- Program: INI Create → Read `EnableDefaultConnectionLimit` → 1 or empty → DefaultConnectionLimit=100; catch → 100
- frmMain Load: log/err log, ReadINI, ShowINI, temp mkdir, Worker UI create
- AutoStart: RunAutoStart=1 && checked worker → scheduler start; else if no checked → log skip
- Title: ProductVersion + General Title
- System menu Close disabled
